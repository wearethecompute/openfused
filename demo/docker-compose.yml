services:
  demo-bot:
    build:
      context: ..
      dockerfile: demo/Dockerfile
    container_name: openfuse-demo
    ports:
      - "2053:2053"
    environment:
      - AGENT_NAME=demo-bot
    volumes:
      - demo-store:/store
    restart: unless-stopped

volumes:
  demo-store:
